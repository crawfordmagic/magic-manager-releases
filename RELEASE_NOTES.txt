Small polish: the Settings icon in the bottom navigation now turns gold when it's the selected tab, matching Leads, Insights, Expenses, and Add. (It was rendering as a fixed grey emoji on some devices; it's now a proper icon that takes the highlight color.)
