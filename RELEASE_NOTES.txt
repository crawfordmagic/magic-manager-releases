Customize your lead statuses — a new Setup → Lead statuses editor lets you add, rename, reorder and remove your own pipeline stages (Booked and Completed stay locked as your income statuses). And importing a client list now keeps each contact's own status from your spreadsheet, instead of marking them all the same — blanks come in as New.
