What's new in this update:

• Smoother first-time setup. A brand-new install now opens the guided setup wizard right away, so a new user lands straight on it instead of seeing the main screen flash by first. (Already-set-up apps are unaffected.)
