Another pass at the Sign-page logo: it now loads directly from your logo's public image link, which renders reliably across browsers and devices. Set it under Settings → Logo with a direct, public image URL.
